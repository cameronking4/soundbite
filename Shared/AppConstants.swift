//
//  AppConstants.swift


import UIKit

struct AppConstants {
    static let exportImageWidth: CGFloat = 2000.0
}

extension UIColor {
    
    static let sbMainBackground: UIColor = UIColor(named: "mainBackground") ?? .white
    
    static let sbAppWhite: UIColor = UIColor(named: "app_white") ?? .white
    
    static let sbPlotBackground: UIColor = UIColor(named: "plotBackground") ?? .white
    static let sbPlot: UIColor = UIColor(named: "plot") ?? .white
}
